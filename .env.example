# Copy to .env and fill in, or set GOOGLE_SOLAR_API_KEY in your deployment (e.g. Vercel) env.
# Needs Maps JavaScript, Geocoding, and Solar APIs enabled. Restrict by HTTP referrer.
GOOGLE_SOLAR_API_KEY=
